{% extends 'base.html.twig' %}

{% block title %}Modifier mon profil - AquaBiome{% endblock %}
{% block page_name %}Édition Profil{% endblock %}

{% block page_icon %}
    {% if app.user and app.user.avatar %}
        <img src="{{ asset('uploads/avatars/' ~ app.user.avatar) }}" alt="Avatar" style="width: 20px; height: 20px; object-fit: cover; border-radius: 50%; vertical-align: middle;">
    {% else %}
        <i class="bi bi-person-circle"></i>
    {% endif %}
{% endblock %}

{% block header_profile_icon %}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/login.css') }}">
    <style>
        .edit-profile-container {
            padding: 20px;
            max-width: 600px;
            margin: 0 auto;
        }
        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }
        .form-label {
            font-weight: bold;
            display: block;
            margin-bottom: 8px;
            color: #333;
        }
        .current-avatar {
            text-align: center;
            margin-bottom: 20px;
        }
        .current-avatar img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid #3f51b5;
        }
    </style>
{% endblock %}

{% block body %}
<div class="edit-profile-container">
    <div class="page">
        <h1>Modifier mon profil</h1>

        <div class="current-avatar">
            {% if app.user.avatar %}
                <img src="{{ asset('uploads/avatars/' ~ app.user.avatar) }}" alt="Avatar actuel">
                <p><small>Image actuelle</small></p>
            {% else %}
                <div style="font-size: 4rem; color: #3f51b5;"><i class="bi bi-person-bounding-box"></i></div>
                <p><small>Aucune image définie</small></p>
            {% endif %}
        </div>
        
        {{ form_start(userForm) }}
            <div class="form-group">
                {{ form_label(userForm.nom, 'Votre Nom', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(userForm.nom) }}
                {{ form_errors(userForm.nom) }}
            </div>

            <div class="form-group">
                {{ form_label(userForm.avatarFile, 'Nouvelle photo de profil', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(userForm.avatarFile) }}
                {{ form_errors(userForm.avatarFile) }}
                <div class="form-text text-muted">Formats acceptés : JPG, PNG, WEBP (Max 5Mo)</div>
            </div>

            <button type="submit" class="btn">Enregistrer les modifications</button>
            <a href="{{ path('gestion_compte') }}" style="display: block; margin-top: 15px; color: #666;">Annuler</a>
        {{ form_end(userForm) }}
    </div>
</div>
{% endblock %}
